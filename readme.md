Dockerのimageをビルドする

`docker-compose build`

Dockerのcontainerを立ち上げる

`docker-compose up -d`

Keycloakの設定のためKeycloak Admin Console にアクセスする

```
localhost:8080/

user name: user
password:  password
```
